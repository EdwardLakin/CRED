import { SuggestionCard } from './SuggestionCard'
import type { EntitySuggestion } from '@/features/evidence/suggestions/data'
export function EntitySuggestionCard({ sessionId, suggestion }: { sessionId: string; suggestion: EntitySuggestion }) { return <SuggestionCard sessionId={sessionId} suggestionId={suggestion.id} category="entity" title={suggestion.display_name} confidence={suggestion.confidence} createdAt={suggestion.created_at} reviewStatus={suggestion.review_status} provenance={suggestion.provenance}><label>Name<input className="input" name="display_name" defaultValue={suggestion.display_name} /></label><label>Description<textarea className="input" name="description" defaultValue={suggestion.description ?? ''} /></label><input type="hidden" name="entity_type" value={suggestion.entity_type} /></SuggestionCard> }
