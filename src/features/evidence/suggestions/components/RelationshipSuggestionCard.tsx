import { SuggestionCard } from './SuggestionCard'
import type { RelationshipSuggestion } from '@/features/evidence/suggestions/data'
export function RelationshipSuggestionCard({ sessionId, suggestion }: { sessionId: string; suggestion: RelationshipSuggestion }) { return <SuggestionCard sessionId={sessionId} suggestionId={suggestion.id} category="relationship" title={`${suggestion.source_type} ${suggestion.relationship_type} ${suggestion.target_type}`} confidence={suggestion.confidence} createdAt={suggestion.created_at} reviewStatus={suggestion.review_status} provenance={suggestion.provenance}><label>Label<input className="input" name="relationship_label" defaultValue={suggestion.relationship_label ?? ''} /></label><input type="hidden" name="source_type" value={suggestion.source_type} /><input type="hidden" name="target_type" value={suggestion.target_type} /><input type="hidden" name="relationship_type" value={suggestion.relationship_type} /></SuggestionCard> }
