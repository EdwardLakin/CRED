import { SuggestionCard } from './SuggestionCard'
import type { TimelineSuggestion } from '@/features/evidence/suggestions/data'
export function TimelineSuggestionCard({ sessionId, suggestion }: { sessionId: string; suggestion: TimelineSuggestion }) { return <SuggestionCard sessionId={sessionId} suggestionId={suggestion.id} category="timeline" title={suggestion.title} confidence={suggestion.confidence} createdAt={suggestion.created_at} reviewStatus={suggestion.review_status} provenance={suggestion.provenance}><label>Title<input className="input" name="title" defaultValue={suggestion.title} /></label><label>Description<textarea className="input" name="description" defaultValue={suggestion.description ?? ''} /></label><label>Event date<input className="input" name="event_start_at" defaultValue={suggestion.event_start_at ?? ''} /></label><input type="hidden" name="event_date_precision" value={suggestion.event_date_precision} /></SuggestionCard> }
