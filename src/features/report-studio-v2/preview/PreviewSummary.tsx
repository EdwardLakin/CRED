/* eslint-disable @typescript-eslint/no-explicit-any */
export function PreviewSummary({brand,session,activeSection,setActiveSection}:any){const rs=brand.report_style; const summary=String(session?.report_summary ?? "").trim(); return <section data-edit-key="summary" data-report-summary-source="ai_report_drafts.summary" className={`rsv2-section ${activeSection==='summary'?'active':''}`} onClick={()=>setActiveSection('summary')}><div className="rsv2-section-heading">{rs.showSectionNumbers&&<span>1</span>}<h2>Executive Summary</h2></div>{rs.showSectionDividers&&<hr/>}<p>{summary || "Executive summary is empty. Add customer-facing summary text here; export will use a deterministic fallback only until a summary is saved."}</p></section>}
