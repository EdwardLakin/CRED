/* eslint-disable @typescript-eslint/no-explicit-any, @next/next/no-img-element */
function safeEvidenceText(e:any){return e?.note || e?.label || e?.mediaKind || 'Evidence item awaiting reviewed note.'}
function img(e:any){return e?.thumbnailUrl?<img className="rsv2-evidence-img" src={e.thumbnailUrl} alt={e.label || 'Evidence image'}/>:<span className="rsv2-evidence-img">No image</span>}
export function PreviewEvidence({brand,session,activeSection,setActiveSection}:any){const rs=brand.report_style;const items=(session?.evidence?.length?session.evidence:[null,null]).slice(0,4);return <button type="button" className={`rsv2-section rsv2-evidence ${activeSection==='evidence'?'active':''}`} onClick={()=>setActiveSection('evidence')}><h2>Evidence</h2><div className="rsv2-evidence-list">{items.map((e:any,i:number)=><article key={e?.id??i} className="rsv2-evidence-item"><div className="rsv2-evidence-copy">{rs.evidenceNumbering&&<b>EV-{String(i+1).padStart(3,'0')}</b>}<p>{rs.notes?safeEvidenceText(e):(e?.label||'Evidence item')}</p>{rs.timestamps&&<small>Captured {new Date().toLocaleDateString()}</small>}{rs.captureMetadata&&<small>{e?.mediaKind??'image'} · reviewed report evidence</small>}</div>{img(e)}</article>)}</div></button>}
