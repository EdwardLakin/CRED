/* eslint-disable @typescript-eslint/no-explicit-any */
import { Check, Select } from './ControlsShared';import { EVIDENCE_IMAGE_SIZES,EVIDENCE_STYLES } from '@/features/branding/types'; export function EvidenceControls({brand,patch}:any){const rs=brand.report_style,p=(x:any)=>patch({...brand,report_style:{...rs,...x}});return <><Select label="Item layout" value={rs.evidenceStyle} onChange={(v:string)=>p({evidenceStyle:v})}>{EVIDENCE_STYLES.map(x=><option key={x}>{x}</option>)}</Select><Select label="Photo size" value={rs.evidenceImageSize} onChange={(v:string)=>p({evidenceImageSize:v})}>{EVIDENCE_IMAGE_SIZES.map(x=><option key={x}>{x}</option>)}</Select><Check label="Item numbering" checked={rs.evidenceNumbering} onChange={(v:boolean)=>p({evidenceNumbering:v})}/><Check label="Source Index (advanced)" checked={rs.evidenceAppendix} onChange={(v:boolean)=>p({evidenceAppendix:v})}/><Check label="Timestamps" checked={rs.timestamps} onChange={(v:boolean)=>p({timestamps:v})}/><Check label="Metadata" checked={rs.captureMetadata} onChange={(v:boolean)=>p({captureMetadata:v})}/><Check label="Notes" checked={rs.notes} onChange={(v:boolean)=>p({notes:v})}/></>}
