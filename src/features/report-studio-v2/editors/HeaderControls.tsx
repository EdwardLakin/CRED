/* eslint-disable @typescript-eslint/no-explicit-any */
import { Check, Color, Select } from './ControlsShared';import { HEADER_LAYOUTS } from '@/features/branding/types';import { SAFE_FONT_STACKS } from '../constants'; export function HeaderControls({brand,patch}:any){const p=(x:any)=>patch({...brand,...x});return <><Select label="Layout" value={brand.header_layout} onChange={(v:string)=>p({header_layout:v})}>{HEADER_LAYOUTS.map(x=><option key={x}>{x}</option>)}</Select><Color label="Background color" value={brand.colors.headerBackground} onChange={(v:string)=>p({colors:{...brand.colors,headerBackground:v}})}/><Check label="Gradient preset" checked={brand.report_style.headerOptions?.gradientPreset} onChange={(v:boolean)=>p({report_style:{...brand.report_style,headerOptions:{...brand.report_style.headerOptions,gradientPreset:v}}})}/><Color label="Text color" value={brand.colors.headerText} onChange={(v:string)=>p({colors:{...brand.colors,headerText:v}})}/><Color label="Divider/accent color" value={brand.colors.primary} onChange={(v:string)=>p({colors:{...brand.colors,primary:v}})}/><Select label="Font family" value={(brand.typography as any).areaStacks?.header??brand.typography.headingStack} onChange={(v:string)=>p({typography:{...brand.typography,areaStacks:{...(brand.typography as any).areaStacks,header:v}}})}>{SAFE_FONT_STACKS.map(f=><option key={f}>{f}</option>)}</Select><Select label="Font weight" value={brand.typography.headingWeight} onChange={(v:string)=>p({typography:{...brand.typography,headingWeight:Number(v)}})}><option>400</option><option>600</option><option>700</option><option>800</option><option>900</option></Select></>}
