/* eslint-disable @typescript-eslint/no-explicit-any */
export const bool=(v:boolean)=>v; export function Select({label,value,onChange,children}:any){return <label className="rsv2-field"><span>{label}</span><select value={value} onChange={(e)=>onChange(e.target.value)}>{children}</select></label>} export function Check({label,checked,onChange}:any){return <label className="rsv2-check"><input type="checkbox" checked={checked} onChange={(e)=>onChange(e.target.checked)}/><span>{label}</span></label>} export function Text({label,value,onChange}:any){return <label className="rsv2-field"><span>{label}</span><input value={value??''} onChange={(e)=>onChange(e.target.value)}/></label>} export function Color({label,value,onChange}:any){return <label className="rsv2-field rsv2-color-field"><span>{label}</span><input type="color" value={value} onChange={(e)=>onChange(e.target.value)}/><input value={value} onChange={(e)=>onChange(e.target.value)}/></label>}
