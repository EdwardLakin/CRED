/* eslint-disable @typescript-eslint/no-explicit-any */
import { TYPOGRAPHY_PRESETS } from '@/features/branding/types';import { SAFE_FONT_STACKS, TYPOGRAPHY_AREAS } from '../constants';import { Select } from './ControlsShared'; export function TypographyControls({brand,patch}:any){const p=(typography:any)=>patch({...brand,typography:{...brand.typography,...typography}});return <><Select label="Preset" value={brand.typography.preset} onChange={(v:string)=>p({preset:v})}>{TYPOGRAPHY_PRESETS.map(x=><option key={x}>{x}</option>)}</Select>{TYPOGRAPHY_AREAS.map(([key,label])=><Select key={key} label={`${label} font`} value={(brand.typography as any).areaStacks?.[key]??brand.typography.bodyStack} onChange={(v:string)=>p({areaStacks:{...(brand.typography as any).areaStacks,[key]:v}})}>{SAFE_FONT_STACKS.map(f=><option key={f}>{f}</option>)}</Select>)}</>}
