/* eslint-disable @typescript-eslint/no-explicit-any */
import { REPORT_TEMPLATES, BRAND_PALETTES, TYPOGRAPHY_OPTIONS, DEFAULT_REPORT_STYLE, normalizeBrandProfile, type TypographyPreset } from '@/features/branding/types';
export function ReportTemplateDrawer({templates,onClose,onApply}:any){return <div className="rsv2-drawer-backdrop"><aside className="rsv2-drawer"><button type="button" className="button button-secondary" onClick={onClose}>Close</button><h2>System Templates</h2>{REPORT_TEMPLATES.map((t:any)=><button type="button" key={t.name} className="rsv2-template-row" onClick={()=>{const palette=BRAND_PALETTES.find(p=>p.name===t.palette)?.colors;onApply(normalizeBrandProfile({colors:palette,typography:TYPOGRAPHY_OPTIONS[t.typography as TypographyPreset],header_layout:t.header_layout,report_style:{...DEFAULT_REPORT_STYLE,sectionStyle:t.sectionStyle,evidenceStyle:t.evidenceStyle,templatePack:t.name}}),`system:${t.name}`)}}><b>{t.name}</b><span>{t.description}</span></button>)}<h2>Saved Custom Templates</h2>{templates.length?templates.map((t:any)=><button type="button" key={t.id} className="rsv2-template-row" onClick={()=>onApply(t,t.id)}><b>{t.name}</b><span>{t.description??'Custom saved template'}</span></button>):<p>No saved custom templates yet.</p>}</aside></div>}
